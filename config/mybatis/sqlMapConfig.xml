<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 加载映射文件 
		由于使用Spring和Mybatis的整合包 进行mapper扫描，这里不需要配置了。
		必须遵循：mapper.xml 和 mapper.java 文件同名且在同一个目录
	 
	<mappers>
		<mapper resource="sqlmap/UserMapper.xml" />
	</mappers>
	-->
	<!-- 配置别名 -->
	<typeAliases>
		<!-- 批量扫描别名 -->
		<package name="com.xiaoyang.po"/>
	</typeAliases>
		
</configuration>
